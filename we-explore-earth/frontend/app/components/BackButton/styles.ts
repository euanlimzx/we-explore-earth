import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    backButton: {
        position: 'absolute',
        top: 50,
        left: 20,
        padding: 10,
    },
    backText: {
        fontSize: 16,
        color: '#007AFF',
    },
});